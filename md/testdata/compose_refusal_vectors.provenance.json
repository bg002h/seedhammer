{
  "_comment": [
    "PROVENANCE PIN for the primary's compose REFUSAL vectors, which are pinned",
    "SEPARATELY from the rest of the compose corpus. Generated by",
    "scripts/vendor-compose-refusal-vectors.sh; never edited by hand.",
    "",
    "WHY A SECOND PIN AND NOT A RE-VENDOR. compose_vectors.provenance.json pins",
    "the whole corpus at ONE commit, and the commit this vector arrives on",
    "(md-codec 0.45.0) also carries md-codec 0.44.0, `a rendered xpub's header",
    "must agree with its origin`, which rewrote the xpubs in all 33",
    "keyed_compose_*.conformance.json records. That rule is not ported here.",
    "",
    "MEASURED, because the reason has to be a fact and not a worry: a full",
    "re-vendor to 4de55155 leaves `go test ./md/` GREEN -- the conformance gate",
    "parses .chains[].descriptor into a struct field it never asserts, so it",
    "cannot see the rewrite. A fresh pin would therefore have imported an",
    "unported normative change with every gate still reporting agreement. The",
    "corpus stays at its own commit until 0.44.0 is ported on purpose; this",
    "file carries the one vector the fable r0 fold needs, at the commit it",
    "actually came from.",
    "",
    "TO RE-PIN: scripts/vendor-compose-refusal-vectors.sh /path/to/descriptor-mnemonic",
    "RE-VENDORED at the fold's round-1 review (I-1/M-3). The primary moved after the first vendoring: dd7cdffc ordered the key-less cap AFTER TooManySlots and LegacyWrapperShape -- their remedies are not cured by \"fold them into one path\" -- and added four precedence_* cases plus a tightened refused_when (key-less = keys == null AND hash != null). The file is byte-identical at dd7cdffc and at the merge b2c5d693; b2c5d693 is what is pinned because it is on main and tagged."
  ],
  "repo": "descriptor-mnemonic",
  "remote": "git@github.com:bg002h/descriptor-mnemonic.git",
  "commit": "b2c5d69384327265da239dc13502727c8f9645fb",
  "commit_on_branch": "main (md-codec 0.45.0 / md-cli 0.17.0, tagged)",
  "path": "crates/md-codec/tests/vectors",
  "files": [
    {
      "name": "compose_refusal_keyless_cap.json",
      "sha256": "7b6505f75cc1fc937fd65279fe200129746cf1534c3fe3adf2e8c9d81ce1de8f"
    }
  ],
  "file_count": 1,
  "vectors": 1,
  "recorded_at": "2026-09-19",
  "repo_clean_when_recorded": true
}
